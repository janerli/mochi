// Intentionally empty for v0 — no Node/Electron APIs are exposed to the
// renderer yet. Add contextBridge.exposeInMainWorld(...) here when desktop-only
// features (tray, native notifications, global shortcuts) are introduced.
